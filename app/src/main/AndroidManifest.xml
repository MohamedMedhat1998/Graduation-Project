<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mohamed.medhat.graduation_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.mohamed.medhat.graduation_project.dagger.DaggerApplication"
        android:theme="@style/Theme.GraduationProject">
        <activity android:name="com.mohamed.medhat.graduation_project.ui.login_activity.LoginActivity" />
        <activity android:name="com.mohamed.medhat.graduation_project.ui.splash_activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.mohamed.medhat.graduation_project.MainActivity" />
    </application>

</manifest>